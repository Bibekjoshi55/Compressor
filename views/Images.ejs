<!DOCTYPE html>
<html>

<head>
  <title>My Images Gallery</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-purple-100 to-indigo-50 p-6">

  <h1 class="text-3xl font-bold text-purple-700 mb-6 text-center">Your Compressed Images</h1>

  <section class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-6">
    <% if (images.length===0) { %>
      <p class="text-center text-gray-500 col-span-full">
        No images uploaded yet. Go back to <a href="/" class="text-purple-600 underline  rounded  border-2 no-underline
 text-3xl">upload</a> some!
      </p>
      <% } else { %>
        <% images.forEach(img=> { %>
          <div class="bg-white rounded-xl shadow-lg p-4 hover:shadow-xl transition relative flex flex-col">
            <img src="<%= img.compressedPath %>" alt="<%= img.originalName %>"
              class="w-full h-48 object-cover rounded mb-3" />
            <h2 class="font-semibold text-purple-600 truncate mb-1">
              <%= img.originalName %>
            </h2>
            <p class="text-sm text-gray-500 mb-3">
              Original: <%= (img.originalSize/1024).toFixed(2) %> KB<br>
                Compressed: <%= (img.compressedSize/1024).toFixed(2) %> KB
            </p>

            <div class="mt-auto flex gap-2">
              <form action="/images/<%= img._id %>/compress" method="POST" class="flex-1">
                <button type="submit"
                  class="w-full bg-purple-500 text-white py-1 rounded hover:bg-purple-600 transition">
                  Recompress
                </button>
              </form>

              <a href="<%= img.compressedPath %>" download
                class="flex-1 text-center bg-green-500 text-white py-1 rounded hover:bg-green-600 transition">
                Download
              </a>
              <form action="/images/<%= img._id %>/delete" method="POST" class="flex-1">
                <button type="submit" class="w-full bg-red-500 text-white py-1 rounded hover:bg-red-600 transition">
                  Delete
                </button>
              </form>
            </div>
          </div>
          <% }) %>
            <% } %>
  </section>

  <footer class="mt-8 text-center text-gray-500 text-sm">
    Compressed images with download option
  </footer>

</body>

</html>